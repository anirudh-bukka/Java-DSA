package arrays;

import java.util.Arrays;

public class UnionOfSortedArrays {

    // USE HASHSET

    public static void main(String[] args) {
        int[] ar1 = {1, 2, 3, 4};
        int[] ar2 = {2, 3, 4, 5};

//        System.out.println(Arrays.toString(union(ar1, ar2)));
//        System.out.println(Arrays.toString(intersection(ar1, ar2)));
    }

    // we do not have to define a new function to verify if an element exists in both or not.

}
