package subset_subsequence;
import java.util.*;

public class ConstructRectangle {
    public static void main(String[] args) {
        int area = 4;
        System.out.println(Arrays.toString(constructRectangle(area)));
    }

    static int constructRectangle(int a, int l) {
        l = (int)(Math.sqrt(a));
        int w = (int)(a/l);

        if(l*w == a) {

        } else {

        }

    }
}
